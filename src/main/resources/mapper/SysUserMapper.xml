<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccc.dao.mapper.SysUserMapper">


    <update id="modifySysUser" parameterType="com.ccc.entity.SysUser">
        UPDATE t_sys_user
        SET
        sys_user_name = #{sysUserName},
        sys_user_pwd = #{sysUserPwd},
        sys_user_status = #{sysUserStatus},
        role_id = #{roleId}
        WHERE sys_user_id = #{sysUserId}
    </update>
    <select id="querySysUser" resultType="com.ccc.entity.SysUser"
            parameterType="com.ccc.entity.SysUser">
        SELECT * FROM t_sys_user
        WHERE
        <if test="sysUserName != null and sysUserName != ''">
            sys_user_name = #{sysUserName}
        </if>
        <if test="sysUserId != null and sysUserId != ''">
            AND sys_user_id = #{sysUserId}
        </if>
    </select>
</mapper>